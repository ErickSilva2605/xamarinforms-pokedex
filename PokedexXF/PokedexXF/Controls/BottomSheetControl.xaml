<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="PokedexXF.Controls.BottomSheetControl"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    x:Name="bottomSheet"
    >

    <ContentView.Content>
        <Grid
            x:Name="gridSheet"
            Margin="{Binding SheetMargin, Source={x:Reference bottomSheet}}"
            HorizontalOptions="FillAndExpand"
            RowSpacing="5"
            VerticalOptions="End"
            >
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <BoxView
                BackgroundColor="White"
                CornerRadius="3"
                HeightRequest="6"
                HorizontalOptions="Center"
                VerticalOptions="End"
                WidthRequest="80"
                />
            <pancake:PancakeView
                Grid.Row="1"
                BackgroundColor="White"
                Content="{Binding SheetContent, Source={x:Reference bottomSheet}}"
                CornerRadius="30,30,0,0"
                />
            <Grid.GestureRecognizers>
                <PanGestureRecognizer PanUpdated="OnPanUpdated" />
            </Grid.GestureRecognizers>
        </Grid>
    </ContentView.Content>
</ContentView>